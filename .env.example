# Copy this file to .env and replace the two placeholder values.
# This file is safe to commit because it contains no real secret.

MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=local-minio-user
MINIO_SECRET_KEY=replace-with-a-local-minio-password
MINIO_BUCKET=bronze

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=erpm
POSTGRES_PASSWORD=replace-with-a-local-postgres-password
POSTGRES_DB=warehouse
